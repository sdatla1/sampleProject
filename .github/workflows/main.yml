on: [push]

jobs:
  Welcome_job:
    runs-on: ubuntu-latest
    name: Welcome Job
    steps:
      - name: Welcome action step
        id: Welcome
        uses: sdatla1/sampleProject@v1
        with:
          who-to-greet: 'Mike'
      # Use the output from the `Welcome` step
      - name: Get the output time
        run: echo "The time was ${{ steps.Welcome.outputs.time }}"